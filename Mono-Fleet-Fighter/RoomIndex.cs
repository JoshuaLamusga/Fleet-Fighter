namespace FleetFighter
{
    /// <summary>
    /// Each constant identifies a 'room' based on its content.
    /// Drawing is separated into different rooms.
    /// </summary>
    public enum RoomIndex { rmTitle, rmOps, rmInfo, rmPlay, rmHighscore, rmCredits};
}
